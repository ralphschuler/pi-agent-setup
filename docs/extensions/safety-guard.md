# Safety guard

`extensions/safety-guard/` protects against dangerous shell commands.

## Behavior

The extension asks for confirmation before destructive commands such as dangerous root deletes.

## Purpose

It reduces accidental damage while preserving the ability to approve intentional risky operations.
